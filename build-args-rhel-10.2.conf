# Variant-specific build arguments.
# Pass to buildah/podman using `--build-arg-file`.

ID=rhel
NAME=rhcos
STREAM=rhel-10.2
DESCRIPTION=Red Hat Enterprise Linux CoreOS 10.2
VERSION=10.2
MUTATE_OS_RELEASE=10.2
BUILDER_IMG=registry.stage.redhat.io/rhel10/rhel-bootc:10.2@sha256:792e011fc7b837415380ba317f7ece20be5237729972ebfae23e38b3935a2ce3
MANIFEST=manifest-rhel-10.2.yaml
IMAGE_CONFIG=image-rhel-10.2.yaml
# XXX: see inject_passwd_group() in build-rootfs
PASSWD_GROUP_DIR=.
# Can't inject openshift version labels for now until
# we support dual stream more properly.
# https://issues.redhat.com/browse/COS-3910
# INJECT_OPENSHIFT_VERSION_LABELS=1
